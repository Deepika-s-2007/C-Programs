include <stdio.h>

int main() {
    float base, height, area;
    scanf("%f %f", &base, &height);
    area = (base * height) / 2;
    printf("Area of the triangle is %.0f sq. units\n", area);
    return 0;
}
